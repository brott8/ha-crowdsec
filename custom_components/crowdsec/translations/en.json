{
  "config": {
    "step": {
      "user": {
        "title": "CrowdSec LAPI Setup",
        "description": "Enter the connection details for your CrowdSec Local API.",
        "data": {
          "host": "Host",
          "port": "Port",
          "api_key": "API Key",
          "scheme": "Scheme (http or https)",
          "scan_interval": "Scan Interval (seconds)",
          "geo_provider": "Geolocation of banned IPs"
        },
        "data_description": {
          "geo_provider": "Disabled by default: no banned IP leaves Home Assistant. The local provider reads crowdsec/GeoLite2-City.mmdb (and, if present, GeoLite2-ASN.mmdb) from the configuration directory, offline; a remote provider sends the banned IPs to that third-party service."
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the CrowdSec LAPI. Check the host, port, and API key.",
      "unknown": "An unknown error occurred."
    },
    "abort": {
      "already_configured": "This CrowdSec instance is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "CrowdSec LAPI Options",
        "data": {
          "scan_interval": "Update frequency (seconds)",
          "geo_provider": "Geolocation of banned IPs"
        },
        "data_description": {
          "geo_provider": "Disabled by default: no banned IP leaves Home Assistant. The local provider reads crowdsec/GeoLite2-City.mmdb (and, if present, GeoLite2-ASN.mmdb) from the configuration directory, offline; a remote provider sends the banned IPs to that third-party service."
        }
      }
    }
  },
  "selector": {
    "geo_provider": {
      "options": {
        "none": "Disabled",
        "geolite2_file": "GeoLite2 file (local, offline, free)",
        "ipquery": "ipquery.io (remote, HTTPS, free)",
        "ip_api": "ip-api.com (remote, HTTP, free)"
      }
    }
  },
  "device_automation": {
    "trigger_type": {
      "new_decision": "A new decision is detected",
      "decision_removed": "A decision is expired"
    }
  }
}
